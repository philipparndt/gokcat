# gokcat systems — list configured systems

List all system aliases configured under `~/.config/gokcat/`.

Each subdirectory containing a `config.json` file is shown as a configured system.

## Usage

```
gokcat systems
```

## Examples

```bash
gokcat systems
```
