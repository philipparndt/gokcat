{
  "name": "setup-gokcat",
  "version": "1.0.0",
  "description": "GitHub Action to download and install the latest gokcat binary with caching support.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "ncc build src/index.ts -o dist"
  },
  "dependencies": {
    "@actions/cache": "^3.2.3",
    "@actions/core": "^1.10.0",
    "@actions/io": "^1.1.2",
    "node-fetch": "^2.6.7",
    "tmp-promise": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@types/node-fetch": "^2.6.12",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.5"
  }
}
